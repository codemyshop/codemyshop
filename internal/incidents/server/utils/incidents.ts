/**
 * Build resolution stub for AGPL distribution.
 * No-op exports — runtime code paths are gated by feature flags.
 */

export async function aggregateAgentsByWeeks(...args: any[]): Promise<any> { return undefined }
export async function aggregateWeekly(...args: any[]): Promise<any> { return undefined }
